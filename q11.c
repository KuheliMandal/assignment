#include<stdio.h>
int main()
{
    int hour,munites;
    printf(" enter value in hh:mm format\n");
    scanf("%d%d",&hour,&munites);
    printf("hour %d and %d munites%d", hour,munites);
    return 0;
}